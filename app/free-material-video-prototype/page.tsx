/**
 * THROWAWAY PROTOTYPE — 自由素材视频入口的交互审稿页。
 *
 * 纯前端 mock，不调用 API、数据库或 localStorage。确认方向后应删除，
 * 或把被选中的交互决定重新实现进正式 VideoGenerationPanel。
 */
import FreeMaterialVideoPrototype from './FreeMaterialVideoPrototype';
import './prototype.css';

export default function FreeMaterialVideoPrototypePage() {
  return <FreeMaterialVideoPrototype />;
}
