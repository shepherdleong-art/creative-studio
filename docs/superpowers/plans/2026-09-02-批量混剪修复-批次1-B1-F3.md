# 批次 1：B1 斜体渲染 + F3 素材使用次数

> 本文件是 `2026-09-02-批量混剪修复与脚本增强-执行文档.md` 的**第 1 批施工分片**，内容摘自母文档并与之保持一致。
> 执行者只按本文件施工即可，**不必也不要**去读母文档里的其它任务卡。
> 行号基于 2026-09-02 的工作区状态，若漂移请按函数名/语义重新定位。

## 本批范围

本批是整份执行文档的**第 1 批**，前面没有依赖，可以直接开工。

两件任务**文件不重叠**，谁先做都行：

| 任务 | 优先级 | 一句话 | 主要文件 |
| --- | --- | --- | --- |
| B1 | P0 | 斜体在正式渲染（sharp/librsvg）中丢失，预览（浏览器）却正常 | `lib/media-core/cover-title-svg.ts`、`lib/batch-production/phase-e.ts` + 2 个测试 |
| F3 | P2 | 「调整片段」素材列表显示使用次数（本片 ×N / 其他 ×M） | `lib/batch-production/output-arrangement.ts`、`components/batch-production/BatchOutputEditor.tsx` |

这两件都是范围清楚、有明确测试靶子的改动——**本批的完成质量决定后面两批要不要继续按同样方式外包**，请严格按验收清单自评。

**本批不需要数据库迁移。**若你认为需要加表加列，停下来报告，不要自行加。

---

## 通用红线（所有任务必须遵守）

- **数据库迁移只能追加**：核心表 `CORE_DB_MIGRATIONS`、成片 `FINAL_EDIT_MIGRATIONS`、批量 `lib/batch-production/schema.ts`（当前最新 v24）三种流都**不许修改已发布条目**；批量迁移必须过 `lib/schema-upgrade/` 的共享备份/跨进程锁/审计 gate。本批任务**都不需要数据库迁移**，若执行中发现需要加表加列，先停下来与需求方确认。
- `lib/batch-production/` **只从 `media-core/` 导入，绝不从 `final-edit/` 导入**。
- 所有本地文件路径一律走 `dataRoot()`，禁止硬编码 `data/`、`storage/`。
- 日志脱敏：新增日志点不打印请求头、密钥或完整鉴权串。
- **改动某个模块后，优先跑与它同名的测试文件**（`node scripts/<name>.test.ts`，Node 22+ 原生跑 TS，无测试框架，`node:assert/strict`）。
- UI 文案为中文；新增颜色一律走设计令牌（`app/globals.css` 末尾暗色覆盖块），不要新增硬编码浅色值。
- 静音占位素材只能预览，不得通过正式发布。

---

## 任务 B1（P0）：斜体在正式渲染中丢失

### 现象

用户在**第二步「脚本与口播」**的批次级字幕样式面板勾选「斜体」并随批次锁定。之后：第二步面板预览（斜体✓）→ 第三步预览弹窗（直立✗）→ 「调整片段」编辑器（斜体✓）→ 导出文件（直立✗）。

### 根因（已三重验证）

1. **数据链路无问题**：数据库 `batch_production_versions.defaultsJson.subtitleStyles.italic = true` 已正确冻结（该批次仅 v1，不存在"改样式产生新版本但渲染用旧版"）。
2. SVG 生成正确携带 `font-style="italic"`：`lib/media-core/cover-title-svg.ts` 第 52 行 `italicAttr`。
3. **死因**：sharp 依赖的 librsvg/pango 渲染管线**不会为没有 italic 字面的字体合成斜体**。实测（复现脚本 `outputs/diag-italic-render.ts`）：
   - `Alibaba PuHuiTi 3.0`、`Microsoft YaHei`：CJK 部分 italic true/false 渲染**完全一致**（直立）；
   - 拉丁字符走字体回退能找到 italic 字面 → 斜体；
   - `Arial`（自带 italic 字面）→ 正常斜体。
4. 浏览器预览（第二步面板、调整片段编辑器）走 CSS，**浏览器会合成斜体**——两处预览与正式渲染表现不一致的根源。
5. 附带发现：封面标题的"斜体"是**假象**——封面字体 `PingFang SC` 在 Windows 不存在，静默回退到**楷体**（天生斜势），与 italic 标志无关（`outputs/diag-italic-pingfang.ts` 实测 true/false 逐像素一致）。该问题单列 B5。

### 修改方案（已实测可行）

把斜体实现从 `font-style="italic"` 改为**绕文字锚点的 `skewX(-12)` 剪切**，改动只在 `lib/media-core/cover-title-svg.ts` 的 `textStyleToSvgElements`（第 42 行起）一处：

- 现状：每个文字层是 `<text x="{px}" y="{py}" ... font-style="italic">`（阴影开启时两层：偏移下层 + 正文层，第 57-66 行）。
- 改为：当 `style.italic === true` 时，每层包成 `<g transform="translate({px},{py}) skewX(-12)"><text x="0" y="0" ...（不带 font-style）>...</text></g>`；`italic === false` 时保持现有输出不变。
- **去掉 `font-style` 属性**，预览（浏览器）与渲染（librsvg）统一走剪切合成——该函数的设计初衷就是“预览与成片同一份实现”。B1 只保证斜体开关、倾斜方向和角度一致；字体字形是否完全一致仍受各平台字体可用性与 B5 的 fallback 问题影响，不宣称全面 WYSIWYG。
- 取舍（已知并接受）：`Arial` 等有真斜体面的字体也统一走合成，不再使用原生斜体字形。本产品为 CJK 场景，可接受。
- skew 角度固定为 `-12`；本任务不再开放角度分支，边界问题按下一条的补偿规则解决。（2026-09-02 复核在本机 sharp/libvips 8.17.3 上实测：`<g transform="translate(px,py) skewX(-12)"><text x="0" y="0">` 经 librsvg 正常生效，与直立版本像素差异显著，机制成立。）
- 描边（`paint-order="stroke fill"`）与阴影层必须随组一起剪切，保持视觉厚度一致。
- 边界不变量固定为：**原本完整落在文字对齐框内的非斜体，开启斜体后仍完整落在同一对齐框内，并保持 left/center/right 的对齐语义**。描边与阴影也计入边界。
- **补偿规则（照此实现，不要自创）**：`italic === true` 时，把参与 fit 的可用宽度先减去 overhang 再缩字号，**锚点坐标不动**——
  ```
  const overhang = style.italic ? fontSize * Math.tan(12 * Math.PI / 180) : 0;  // ≈ fontSize * 0.2126
  const usableWidth = Math.max(1, style.boxWidthPx - overhang);
  // 用 usableWidth（而不是 boxWidthPx）跑现有的 estimateSingleLineWidth 缩放
  ```
  因为 fontSize 会被缩放回写，overhang 要用**缩放后的最终 fontSize** 迭代一次（先按初始 fontSize 估 overhang → 缩放 → 用新 fontSize 重算 overhang → 再缩一次即可收敛，不必循环）。
  移锚点会破坏 left/right 的对齐语义，所以只允许缩字号，不允许平移锚点。
  副作用是同一段文字开启斜体后可能比直立版本略小，这是接受的取舍。
- 量级参考（80px 字号、PingFang、开描边实测）：直立 bbox `x0=160 x1=639`，skew 后 `x0=164 x1=647`——字形顶部右移约 17px、底部左移约 2px，与 `fontSize × tan(12°)` 吻合。
- `estimateSingleLineWidth` 本身不改（它只负责按可用宽度缩字号）；overhang 的计算放在调用它之前。
- 这是渲染语义变化：把 `lib/batch-production/phase-e.ts` 的 `BATCH_RENDER_ADAPTER_VERSION` 从 `batch-render-v2` 提升到 `batch-render-v3`，同步更新 `scripts/batch-output-clip-edit.test.ts` 中 3 处精确 requestKey 断言。该常量是**新建渲染任务的幂等契约盐**，不是 attempt 级真实执行版本：旧 v2 任务点「重新生成」仍保留原 requestKey，但新 attempt 会运行当前代码并产出修复后的斜体。已存在候选不会自动改写，需显式重渲染。

### 影响面

本次实际影响的是批量生产中调用该共享函数的路径：

- 批量字幕 overlay：`lib/batch-production/batch-renderer.ts` 第 685-704 行 `materializeSubtitleOverlays`（经第 696 行调用）；
- 批量封面标题：`lib/batch-production/cover-title.ts` 第 116 行；
- 批量浏览器预览：`BatchCoverDraftPreview.tsx`、`BatchStepScripts.tsx`、`BatchTimelinePreview.tsx`。

`final-edit/mixcut` 当前通过浏览器 Canvas 生成文字 overlay，并不调用 `textStyleToSvgElements`，因此不把它们列为本任务的受益面；如需统一单条模式，另立任务验证 Canvas 路径。

### 验证

1. 自己写一个一次性脚本（附录 A 里的 `outputs/diag-*.ts` **已不在本仓库**，`outputs/` 是 gitignored，那次调查在另一台 Windows 机器上跑的；不要去找这些文件）：用冻结样式（Alibaba PuHuiTi 3.0, italic: true）经 sharp 渲染 PNG，肉眼确认 CJK 倾斜。脚本要放在仓库根目录下运行才能 import 到 `node_modules/sharp`。
2. **必须同步改的两处现有断言**（去掉 `font-style` 后它们一定变红，改成 `skewX` 断言，**不要把 `font-style` 加回去、也不要把断言删空**）：
   - `scripts/batch-cover-title.test.ts` 第 128 行 `assert.ok(svg.includes('font-style="italic"'))`
   - `scripts/batch-subtitle-style.test.ts` 第 88 行 `assert.match(svg, /font-style="italic"/)`

   同文件 `batch-cover-title.test.ts` 第 146 行的 `assert.ok(!noShadowNoStroke.includes('font-style'), '非斜体不得出现 font-style')` 是非斜体分支，改完仍然成立，**保持原样**。
   除此之外全仓库再无 `font-style` 引用（只剩 `lib/media-core/cover-title-svg.ts:52` 这个待改的实现点）。
   跑 `node scripts/batch-cover-title.test.ts` 与 `node scripts/batch-subtitle-style.test.ts`；**新增断言**：`italic: true` 时输出包含 `skewX`，`italic: false` 时不包含。
3. 用更大画布先栅格化得到未裁切 alpha bbox，再与目标对齐框比较；覆盖左/中/右对齐、贴近安全边缘的长 CJK、描边和阴影，验证上述不变量，而不是简单断言像素“不碰画布边缘”。
4. 端到端：用斜体批次重新渲染一条成片，确认视频字幕为斜体；另实际栅格化一张批量封面标题，确认 cover-title 路径同样倾斜且无裁切。

---

## 任务 F3（P2）：素材列表显示使用次数

### 根因（已定位，无需迁移）

使用情况由 `getBatchOutputArrangementView`（`lib/batch-production/output-arrangement.ts` 第 425 行）从 `batch_output_versions.arrangementJson.clips[]` 实时派生：第 458-483 行用 `Map<assetId, Set<planId>>` 统计，**Set 把次数丢掉了**——同一素材在同一成片用 3 次也只显示一个「本片已用」。底层 `clips[]` 是数组，用几次有几条记录，**把 Set 换成计数 Map 即可，不需要数据库迁移**。

统计范围固定为当前 `batchVersionId` 下每个 plan 的 `currentVersionId`；历史输出版本、其他批次和其他项目一律不参与。

### 修改方案

- 后端 `output-arrangement.ts`：
  - 第 460-483 行：`Set<planId>` → `Map<planId, number>`（clips 循环里计数++）；
  - 视图类型 `BatchOutputPoolAssetView`（第 51-65 行）增加 `useCountByPlanId: Record<string, number>`；
  - 输出（第 517-518 行）保留 `usedByPlanIds` 兼容，新增计数字段。
- API 路由（`app/api/batch-production/batches/[id]/outputs/[planId]/arrangement/route.ts` 第 26 行）透传，无需改。
- 前端 `components/batch-production/BatchOutputEditor.tsx`：
  - `本片次数 = useCountByPlanId[planId] ?? 0`；`其他次数 = 其余 planId 的计数之和`；两个数字独立计算、独立显示。素材同时被本片和其他成片使用时，必须同时出现「本片已用 ×N」与「其他成片已用 ×M」，不能再用 `!usedByThis` 把后者隐藏；
  - 徽标判断（第 474-476 行）与渲染（第 502-505 行）显示「本片已用 ×N」「其他成片已用 ×M」（次数为 1 时可省略 ×1）；
  - 顶部「已用 / 未用」统计保持现状；本任务只修改每条素材的使用次数徽标，避免把“素材种类数”和“片段出现次数”混成一个数字。
- **产品决策（已与需求方确认口径）**：`split` 把同一素材切成两段会在 clips[] 里产生两条记录，**计为使用 2 次**，接受此口径并在 UI 文案上不做特殊解释。
- 封面使用仍走独立 `coverUsedByPlanIds`，只显示现有「本片封面」标记，不计入片段使用次数，也不在本任务增加封面次数（2026-08-28 计划 A2 项的既有口径，不回退）。同一素材兼作封面和片段时，只累计 `clips[]` 中的出现次数，封面身份额外贡献 0 次。

### 验证

`scripts/batch-output-clip-edit.test.ts`（第 288-315 行现有断言）至少覆盖同一素材“本片 2 次 + 其他成片 3 次”，断言两个徽标同时显示所需的数据；另补 insert/split 计数。再补 UI 契约/组件测试，给定上述数据时两个徽标同时渲染。测试通过后在 `docs/reference/批量生产模块.md` 同步一行说明。

---

## 本批验收清单（自评 + Review 对照）

**B1 斜体**

- [ ] `textStyleToSvgElements` 在 `italic:true` 时输出 `skewX(-12)` 包裹层，且不含 `font-style`；`italic:false` 输出与现状一致（快照不变）
- [ ] 阴影层与正文层都在剪切组内
- [ ] 左/中/右对齐及安全边缘长 CJK 文本无裁切；`BATCH_RENDER_ADAPTER_VERSION` 已提升并同步断言
- [ ] `batch-cover-title.test.ts:128` 与 `batch-subtitle-style.test.ts:88` 两处 `font-style` 断言已改为 `skewX` 断言（不是删空、也不是把 `font-style` 加回去）；`batch-cover-title.test.ts:146` 的非斜体断言保持原样
- [ ] 斜体开启后仍按「先减 overhang 再缩字号、锚点不动」补偿，未平移锚点
- [ ] 新增/更新的测试通过；用斜体批次端到端渲染一条成片，导出视频字幕为斜体
- [ ] 浏览器预览与服务端渲染视觉一致（同走剪切）
- [ ] `docs/reference/批量生产模块.md` 已同步渲染适配器版本与斜体语义

**F3 次数**

- [ ] 徽标显示「本片已用 ×N / 其他成片已用 ×M」（N/M 为 1 时允许省略 ×1）；同一素材同时用于两边时两个徽标并存
- [ ] “本片 2 次 + 其他 3 次”及 split/多次 insert 计数正确；封面不计入片段次数
- [ ] 无数据库迁移；`batch-output-clip-edit.test.ts` 通过；参考文档已同步

**通用**

- [ ] 无新增迁移条目；无跨模块违规导入；日志无敏感信息；同名测试全部通过

---

## 本批交付要求（必须照做）

收尾时**固定输出这两样**，不要只说「已完成」：

1. **跑了哪些测试、结果如何**——命令原样贴出（`node scripts/<name>.test.ts`），失败的把断言信息一起贴。没跑的要说明为什么没跑。
2. **哪些验收点没做到**——对照上面的验收清单逐条自评，做不到的直接说「未做到 + 原因」。**漏报比做不完严重得多**：做不完可以下一轮补，漏报会让错误叠进后续批次。

另外：

- **只改本批任务卡点名的文件。**看到旁边还有别的可优化处，记下来写进报告，不要顺手改——其它任务卡由别的批次负责，动了会撞车。
- 行号漂移属正常，按函数名/语义重新定位即可；但若发现文中**事实/语义**与代码冲突，**先停下来报告**，不要自行改方案。
- 每个任务改完就跑它的同名测试，不要攒到最后一起跑。
- 收尾跑一次 `npm run lint`。

**本批应跑的测试**：

`node scripts/batch-cover-title.test.ts`、`node scripts/batch-subtitle-style.test.ts`、`node scripts/batch-output-clip-edit.test.ts`

---

## 参考

- 母文档（含全部 9 个任务与调查证据索引）：`docs/superpowers/plans/2026-09-02-批量混剪修复与脚本增强-执行文档.md`。**本批不需要读它**，该给的这里已摘全；只在需要背景时再翻。
- 仓库约定看 `AGENTS.md`（结构、命令、红线），领域词汇看 `CONTEXT.md`。
- 深水区参考：`docs/reference/批量生产模块.md`、`docs/reference/详情页智能脚本生成.md`。

## 附录 B：用户环境

Windows 客户端，Electron 开发模式（`npm run dev:desktop`），数据根为项目根 `i:\m7-studio`（`data/workbench.db`）。 sharp 为仓库 node_modules 自带版本。
