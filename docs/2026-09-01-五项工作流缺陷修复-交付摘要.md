# 五项工作流缺陷修复 — 交付摘要

> 日期：2026-09-01（Asia/Shanghai）
> 执行者：GLM 5.3（首轮 C1–C5 实施 + 反馈修复轮）
> 独立复核：Opus 5，见 `docs/2026-09-01-五项工作流缺陷修复-独立复核.md`
> 执行方案：`docs/superpowers/plans/2026-09-01-五项工作流缺陷修复-执行方案.md`

## 1. 基线与提交

- `EXECUTION_BASE`：`24fc339`（执行方案文档提交；审计基线为 `abaadcd`）
- 首轮 HEAD：`3c09c32`（复核范围 `24fc339..3c09c32`）
- 反馈修复轮在 `3c09c32` 之上追加三个修复 commit（见 §10）。

| 卡片 | Commit | 主题 |
| --- | --- | --- |
| C1 | `436b35d` | Expose project scripts in single Mixcut（13 文件，+528/-10） |
| C2 | `c0d49d8` | Allow re-exporting edited Mixcut outputs（8 文件，+244/-33） |
| C3 | `e2211ce` | Unify batch BGM draft application（7 文件，+310/-39） |
| C4 | `94c7fa5` | Stabilize scene result ordering（11 文件，+254/-24） |
| — | `67aecdf` | C3 的 react-compiler lint 修复（C3 验证清单未跑 lint 漏网，全局 lint 跑出后单独提交） |
| C5 | `3c09c32` | Add friendly generated video names（27 文件，+708/-75） |

首轮合计 51 文件、+2041/-176。

## 2. 每张卡的完成条件对照

### C1 项目脚本进入单条混剪（§3.5）

- 前端三处过滤统一走 `isMixcutScriptChoiceVisible`（`lib/final-edit/mixcut-creation-state.ts`），底层复用服务端同一 `lib/media-core/script-visibility.ts` 纯函数。
- `ScriptSnapshot` 增加 `sourceRevisionId/sourceRevisionNumber` 身份，写入 `scriptSnapshotJson`，无数据库迁移；旧快照按 `null` 兼容。
- 「源脚本有新版本 / 同步最新版本」走现有 `set_mixcut_script_state` 自动保存链，同步后用服务端返回身份清除提示。
- §3.4 的 7 条情形全部有测试（含 Playwright 浏览器证据）。

### C2 修改后重新导出（§4.5）

- `parseRenderRevisionFromSnapshot` 只投影 `groupRevision/variantRevision` 两个整数标量；单 job 路由仍删除整个 `inputSnapshotJson`。
- `ExportStep` 按当前 revision / 历史成功 job 分类；旧在途任务不遮住当前导出，「上一版可下载」与「重新导出当前修改」并存。
- §4.4 的 5 步完整回归由 Playwright 覆盖；legacy 缺 revision job 兼容有服务端测试。

### C3 批量 BGM 曲目与参数统一应用（§5.5）

- `musicDraft`（trackId/gainDb/fadeInSec/fadeOutSec）四字段共同弄脏「应用 BGM 更改」；下拉只改草稿。
- 原子命令 `set_music` 一次校验并写入全部参数，只递增一次 `editRevision`；旧 `set_music_track`/`set_music_params` 保留兼容。
- 草稿同步决策抽纯函数 `resolveBgmDraftAfterViewLoad`，静默 `loadView(true)` 不冲掉未应用草稿。
- `deferRender: true` 与编辑轮结束的 `commit_render` 未弱化。
- **换源续播**：首轮实现有 F3 缺陷（关闭→选曲不续播），反馈轮已修复并有真实播放证据（见 §10）。

### C4 场景结果稳定排序（§6.5）

- `CORE_DB_MIGRATIONS` 尾部追加 `jobs.createdAt`、`jobs.creationIndex`；四个 job 创建入口全部写入创建身份；不回填历史行。
- 排序只在 `lib/project-job-order.ts` 实现一次；`sqliteTimestampMs` 兼容 ISO `T` 与空格两种格式；状态不参与排序。
- §6.4 的 6 条情形全部有内存库测试。

### C5 生成视频友好展示名称（§7.5）

- `video_jobs.displayName` 可空列追加迁移；新命名模块 `lib/video-output-filenames.ts`（sanitize 限长，单段 80 / 总长压到 120）。
- 单条与批量创建时持久化；旧行读取时经 `resolveVideoJobDisplayNames` 确定性派生，不回写、不从文件名猜归属。
- 播放 URL 继续走物理 `filename`；`/api/videos/[...path]` 不设 `Content-Disposition`，同源 `<a download={displayName}>` 浏览器认可，无需新增鉴权下载路由。

## 3. 自动测试证据（§8.1 清单，全部实跑）

| 命令 | 结果 |
| --- | --- |
| `node scripts/db-migrations.test.ts` | 通过 |
| `node scripts/script-studio-visibility.test.ts` | 通过 |
| `node scripts/script-studio-pipeline.test.ts` | 通过 |
| `node scripts/final-edit-mixcut-script.test.ts` | 通过 |
| `node scripts/final-edit-mixcut-creation-state.test.ts` | 通过 |
| `node scripts/final-edit-mixcut-flow.test.ts` | 通过 |
| `node scripts/final-edit-mixcut-context-contract.test.ts` | 通过 |
| `node scripts/final-edit-workspace.test.ts` | 通过 |
| `node scripts/final-edit-groups-route-contract.test.mjs` | 通过 |
| `node scripts/final-edit-mixcut-ui-contract.test.mjs` | 通过 |
| `node scripts/final-edit-preview-playback.test.ts` | 通过 |
| `node scripts/batch-output-clip-edit.test.ts` | 通过 |
| `node scripts/batch-phase-e-ui-contract.test.mjs` | 通过 |
| `node scripts/batch-media-catalog.test.ts` | 通过 |
| `node scripts/batch-prepare.test.ts` | 通过 |
| `node scripts/result-gallery-jobs.test.ts` | 通过 |
| `node scripts/project-job-order.test.ts` | 通过 |
| `node scripts/video-output-filenames.test.ts` | 通过 |
| `node scripts/video-bulk-prompt-ui-contract.test.mjs` | 通过 |
| `node scripts/final-edit-mixcut.playwright.test.mjs` | 通过（C1/C2 定向验证各跑一次，全局回归再跑一次） |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | 0 error / 50 warning（全部为基线既有，见 §6） |
| `npm run build` | exit 0。按实际执行顺序：首轮全局回归一次；反馈修复轮一次（在 `71786b3` 的代码改动之后、`fada88e` 的路由改动之前——轮二 G3 指出当时 `.next` 无新于该次构建的文件，`fada88e` 的五个路由文件未进过该次构建，由轮二复核者与轮二修复轮各补跑一次验证 exit 0）；轮二修复轮（G1/G2 改动后）重跑一次，含 standalone 资产同步 |

复核者对全量 227 个测试文件另做扫描：221 通过 / 6 失败，其中 5 个在基线 `24fc339` 上复现同样失败（`batch-preparation-workspace.playwright`、`batch-render-smoke`、`batch-v15-lut-conflict-migration`、`project-info-ui-contract`、`script-route-v3`），`script-studio-scheduler-version` 为既有模块缺失（`lib/script-providers/openai-compatible` 不存在），均与本轮无关；`final-edit-render.test.ts` 未跑通（环境挂死，本轮未改其依赖模块）。

## 4. M1–M5 人工/浏览器验收

| 编号 | 状态 | 证据 |
| --- | --- | --- |
| M1 单条混剪脚本步 | 通过 | `final-edit-mixcut.playwright.test.mjs` 实证：项目脚本可选、旧 group 保留快照、显式同步并清提示 |
| M2 导出→改→重导 | 通过 | 同上：旧下载保留 + 「重新导出当前修改」POST 新 revision + 旧 job URL 仍 200 |
| M3 播放中切 BGM | 通过 | **首轮为零运行时证据（复核 F2）**；反馈轮新增 `batch-bgm-source-switch.playwright.test.mjs`：真实 standalone 服务 + ffmpeg 媒体，覆盖 关闭→选曲、A→B 换源、切回关闭、关闭→再选曲 四条路径，断言 `paused === false`、位置贴合正文播放头（误差 < 1.5s）、画面播放头全程不归零；三连跑通过 |
| M4 场景卡片顺序 | 部分覆盖 | 内存库单测覆盖 §6.4 全部 6 条；无浏览器证据（判定足够） |
| M5 三处名称一致 | 部分覆盖 | 源码匹配 + 单测/链路测试；无浏览器证据 |

## 5. 数据库迁移与新旧库验证

- 追加条目（尾部，未改动任何已发布条目）：`jobs.createdAt TEXT`、`jobs.creationIndex INTEGER NOT NULL DEFAULT 0`、`video_jobs.displayName TEXT`（可空、无默认）。
- `db-migrations.test.ts` 保留旧尾部条目相对顺序断言，并新增三条目的断言；旧库升级后 legacy 行 `createdAt=NULL`/`displayName=NULL` 保持不变，重复迁移幂等。
- 不对用户正式 `workbench.db` 执行人工写验收。

## 6. Lint / build warning 区分

- 首轮与反馈轮 `npm run lint` 均为 0 error / 50 warning。复核者逐条比对基线：改动文件命中的 4 条（`CoverEditorDrawer.tsx`、`ExportStep.tsx`、`PreviewStep.tsx` 的 `no-img-element`，`mixcut-context.ts` 未使用导入）在 `24fc339` 上完全一致，**本轮未引入新 warning**。
- `npm run build` 无新增 warning。

## 7. 范围外的连带改动（复核 F7，此处正式声明）

- C5 触及预计路径之外的两个文件：`lib/batch-production/prepare.ts`（视图补名）、`lib/batch-production/output-arrangement.ts`（池素材视图 `displayName`）；只读视图层，不写 `mediaJson`、不动素材身份指纹。
- C1 的 `ORDER BY COALESCE(r.createdAt, ps.updatedAt) DESC` 连带改变 `creative-package` 导出取「第一条项目脚本」的语义：从最早 revision 变为当前 revision——与该处注释意图一致，属顺手修正。
- `lib/batch-production/media-catalog.ts` 新增 lib 根依赖 `../video-output-filenames.ts`：不碰 `final-edit/`，未破 AGENTS.md 红线字面（该文件本就 import `../data-root.ts`/`../ffmpeg.ts`）。

## 8. 未解决风险与明确未做项

1. `batch-preparation-workspace.playwright.test.mjs` 在基线上已坏（`waitForRequest` 等 POST `assets/analyze` 超时），本轮未修——它是既有问题，建议单独排查。
2. `lib/script-providers/openai-compatible` 模块缺失导致 `script-studio-scheduler-version.test.ts` 失败——既有问题，建议单独排查。
3. `AGENTS.md` 写批量迁移「v1–v23」实际已到 v24，`batch-v15-lut-conflict-migration.test.ts` 期望值未跟上——既有问题，本轮未动。
4. `<a download={displayName}>` 的浏览器/Electron 行为已有同源测试覆盖推断；若日后出现跨源下载场景，再按 §7.2.7 增加鉴权下载路由。
5. M4/M5 的浏览器证据未补（内存库/链路测试判定足够）；如复核方要求，可另行追加。

## 9. 工作树状态

- 首轮交付时：工作树干净（除基线已有的未跟踪交接文档 `docs/2026-08-30-混剪E2E测试选择器修复-交接.md`）。
- 反馈修复轮完成后：`git status --short --branch` 仅有本轮三个修复 commit 之上的交付摘要文档（本文件）与复核报告、既有未跟踪交接文档；`git diff --stat 24fc339..HEAD` 见 §1 与 §10。

## 10. 反馈修复轮（复核 F1–F7 处置）

对 `docs/2026-09-01-五项工作流缺陷修复-独立复核.md` 各 finding 的处置：

| Finding | 处置 | Commit |
| --- | --- | --- |
| F1（P1）交付摘要缺失 | 本文件补齐 | 文档 commit |
| F2（P1）C3 无运行时播放证据 | 新增 `scripts/batch-bgm-source-switch.playwright.test.mjs`（真实服务 + 真实媒体 + 真实播放断言；在旧构建上复现 bug、新构建通过，三连跑稳定） | BGM 修复 commit |
| F3（P2）关闭→选曲不续播 | `BatchTimelinePreview.tsx` 以 `bgmSyncInitializedRef` 区分「首帧」与「BGM 原本关闭」，null→track 走 resume 分支 | 同上 |
| F4（P3）闭包播放头 + 监听器泄漏 | `resumeBgm` 改读 `lastDrivenSecRef` 与 `bgmLevelsInputRef`；`loadedmetadata` 监听器在 effect cleanup 中摘除 | 同上 |
| F5（P3）批量视频批内倒序 | 同批共享一个 `batchCreatedAt`；列表/导出/混剪上下文 `ORDER BY` 统一补 `rowid` 决胜，批内 V01→V02→V03 与 C4 图片规则对齐；`video-output-filenames.test.ts` 加回归 | 排序对齐 commit |
| F6（P3）恒真断言 | 删除 `project-job-order.test.ts` 的恒真断言与未用导入 | 合入排序对齐 commit |
| F7（P3）未报告的连带改动 | 在本文件 §7 正式声明（处置即声明，无需改码） | — |

反馈修复轮定向验证（全部实跑通过）：`batch-bgm-source-switch.playwright.test.mjs`、`batch-phase-e-ui-contract.test.mjs`、`video-output-filenames.test.ts`、`project-job-order.test.ts`、`batch-output-clip-edit.test.ts`、`final-edit-preview-playback.test.ts`、`db-migrations.test.ts`、`batch-media-catalog.test.ts`、`batch-prepare.test.ts`、`result-gallery-jobs.test.ts`、`npx tsc --noEmit`、`npm run lint`（0 error / 50 warning，无新增）、`npm run build`。

## 11. 轮二反馈修复（复核 G1–G3 处置）

对 `docs/2026-09-01-五项工作流缺陷修复-复核轮二.md` 各 finding 的处置（轮二确认 F1–F7 全部成立，含红灯反证）：

| Finding | 处置 | Commit |
| --- | --- | --- |
| G1（P3）rowid 决胜漏三处读取端 | **轮二只改对了三分之二**（`claimNextVideoJob` 与 `prepare.ts`；轮三复核发现 `claimNeedsCheckVideoJob` 漏改，且轮二新增的同批断言用顺序命名 id，在 bug 存在时也绿，属无鉴别力假守护）。轮三补齐：`claimNeedsCheckVideoJob` 的 `ORDER BY createdAt, id` → `rowid` 并对齐函数注释；`video-queue-resume.test.ts` 两段断言改为逆字典序 id 插入（pending：c→b→a；needs_check：b→a），并留注释说明顺序命名会让断言退化成恒真。红灯自证实测：仅应用测试改动时 `needs_check 批内同 createdAt 按 rowid 领取` 失败（actual `'resume-job-a'` / expected `'resume-job-b'`，与复核者探针一致），应用代码修复后通过 | 轮二 commit `8c1d49f`（部分）+ 轮三 commit 补齐 |
| G2（P3）结果卡 status 重排抵消 F5 对齐 | 经产品确认删除 `VideoGenerationResults.tsx` 的 status 排序：卡片完全保留列表 API 顺序（createdAt DESC, rowid ASC），状态变化不跳位，与 C4「状态不参与位置计算」一致；`video-bulk-prompt-ui-contract.test.mjs` 加 `doesNotMatch /\.sort\(/` 守护 | 同上 |
| G3（P3）构建证据口径过强 | 本文件 §3 构建证据改为按实际执行顺序陈述；轮二修复轮的全部代码改动（含 `fada88e` 遗留的五个路由）经重跑 `npm run build` 验证 | 文档 commit |

轮二修复轮定向验证（全部实跑通过）：`video-queue-resume.test.ts`、`video-bulk-prompt-ui-contract.test.mjs`、`batch-prepare.test.ts`、`video-output-filenames.test.ts`、`npx tsc --noEmit`、`npm run lint`（0 error / 50 warning，无新增）、`npm run build`（本轮全部改动之后）。

## 12. 轮三返工（R1 处置）

对 `docs/2026-09-01-五项工作流缺陷修复-轮三返工说明.md` 的处置：

- **代码**：`lib/video-queue.ts` 的 `claimNeedsCheckVideoJob` 补齐 `ORDER BY createdAt, rowid`（轮二漏改的第三处），函数注释补批内决胜口径。
- **测试鉴别力**：`video-queue-resume.test.ts` 的 pending 段与 needs_check 段断言全部改为逆字典序 id 插入，bug 存在时会红；两段各留注释说明顺序命名会让断言退化成恒真。
- **红灯证据**（先测试后代码，两次实跑）：
  1. 仅应用测试改动（`:629` 仍为 `id`）：`node scripts/video-queue-resume.test.ts` exit 1，`AssertionError: needs_check 批内同 createdAt 按 rowid 领取`，actual `'resume-job-a'` / expected `'resume-job-b'`；
  2. 应用代码修复后：exit 0，`video queue resume tests passed`。
- **范围确认**：全仓 `video_jobs` 查询不再有 `ORDER BY createdAt, id`（`project_artifacts`、`batch_asset_sources`、`batch_tasks` 等其他表的同名写法按返工说明 §1.4 不动）。
- **轮三验收**（全部实跑）：`video-queue-resume.test.ts`（红→绿两跑）、`batch-prepare.test.ts`、`video-bulk-prompt-ui-contract.test.mjs`、`npx tsc --noEmit`、`npm run lint`（0 error / 50 warning）、`git diff --check`、`npm run build`（本轮全部改动之后）。

轮二另列的范围外观察（`lib/final-edit/workspace.ts:997` 封面候选 `ORDER BY vj.id` 为 UUID 随机序）与轮一遗留三条既有问题（`openai-compatible` 模块缺失、AGENTS.md 迁移版本号 v23→v24、`batch-preparation-workspace.playwright` 基线即坏）维持 §8 立场：单独立项，不塞进本轮。
