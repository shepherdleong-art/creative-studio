# 脚本标题修复与验证

第三步 Script Studio 曾允许不同正文使用相同脚本标题和封面主标题；知识目录又要求标题包含完整统一名称与搜索词，名称包含型号时会把型号一起带入。模型漏标题时还会用商品名称拼接“口播方案”。

此次改为从当前方向与已核验卖点拟标题：型号仅用于商品匹配，展示名称在生成时清理，搜索词单独保存。原始商品身份、目录来源和既有卖点编排保持不变。缺标题不再用型号兜底，转入仅标题修复。

保存前，脚本标题与封面主标题分别对照同批及同项目最近 30 天的最多 100 个项目脚本版本；单条再生成排除自身全部版本。重复比较允许共享正常品牌和品类词，忽略标点、空白、大小写、末尾编号，并检查保守的近似文本。全部限制详见 `docs/reference/详情页智能脚本生成.md`。

不合格时最多调用两次标题修复，服务端只应用明确不合格的字段，保留正文、字幕、卖点引用、时长与其他合格标题。最终检查与保存共用写事务。失败方案不保存，其他方案可以部分完成。首稿正常并发不变，执行器版本从 9 升至 10；不需要迁移，不更新既有下游快照。

## 验证证据

- 原最小复现由失败转为通过：同标题/同封面不能因正文不同而放行，纯型号不再通过，上一批标题进入生成上下文，漏标题不再生成“型号口播方案”。
- `script-studio-title-repair.test.ts` 使用临时 SQLite 和假供应商覆盖：同批并发与独立封面检查、近期历史、30 天边界/100 版本上限/项目隔离、单条再生成排除自身、旧快照、大小写/全角型号、品牌/尺寸保留、标题长度与事实词项、只修指定字段、两次失败不保存。
- 已保存来源数据的本地离线回放保存在 `outputs/title-repair-acceptance/`，不提交这些本机数据。回放只读取旧记录并在内存中修复，不修改真实数据库，也不产生模型调用。
- 25 个相关 Script Studio 自动回归全部通过，TypeScript 检查、改动范围 ESLint（0 errors）及 `npm run build` / standalone 资源同步通过。仓库已有全量 lint 错误为 `scripts/company-seedance-1080p-probe.ts:94` 的 `no-explicit-any`，该文件不在本次改动范围。

## 验证边界

逻辑回归未调用收费供应商、未运行真实生成 UI、未启动业务调度器，也没有重跑图片提取。独立调度器热替换动态测试本轮未运行。标题事实检查覆盖确定性数字和常见声明词项，不承诺覆盖所有语义改写。另一位使用者的程序版本、参数和完整输出尚未核对，本地代码回归不等于对其环境的完整复现。

## 使用与集成

修复工作区基于日常分支 `feat/video-bulk-prompt` 的 `1b73ff3`，分支为 `codex/script-studio-title-repair`。已用明确的 19 文件补丁整合到日常项目，逐文件 SHA-256 与修复工作区一致；原有 `stop-desktop.command` 改动保留，不创建提交或推送。

经状态文件与 health 实例核对后，使用项目共享停机工具完成优雅关闭。隔离工作区的 standalone 构建已安装到日常 `.next`，旧产物保存在日常 `outputs/title-repair-deployment/next-before-title-repair`；桌面壳编译通过，并通过现有 `start-desktop.command` 恢复运行。没有制作安装包。

新构建 ID 为 `lEy0mzEKsqsBb7FhRaaJR`。编译产物包含标题修复任务、型号检查与封面重复检查标记。重启后的桌面 health / 首页均为 HTTP 200，health 与状态文件实例相符，首页 HTML 包含该构建 ID，服务工作目录确认是日常 `.next/standalone`。运行证据保存在日常 `outputs/title-repair-deployment/runtime-proof.json`。

原生窗口视觉检查尝试被 macOS ScreenCaptureKit 的 -3811 捕获错误阻断；本轮已确认 HTTP 页面与进程/构建归属，未声称桌面逐项点击或真实模型生成已通过。

## 后续真实验收（2026-09-09 21:48–21:52）

用户直接确认外发素材、目的地与调用规模后，已在上述新构建的日常桌面服务提交一次完整的两图首次提取任务：Gemini `gemini-3.8-flash`，OpenAI-compatible 中转 `https://cf.api.fan/`，目标 6 条、每条 15 秒。两张图读取为 65 个切片，随后提取失败：某页第 1–6 个切片批次的两次尝试分别耗时约 120004 / 803 ms，最终收到 HTTP 503。任务总耗时约 214 秒，终态 failed，0 / 6 条产出。

证据核验、脚本生成、标题检查和标题修复均未进入，不能据此宣布真实标题验收通过。原有 9 个脚本版本逐字段核对未变，新脚本和新卖点库修订均为 0。结束后桌面 health / 首页仍为 HTTP 200，实例与构建 ID 匹配。详细记录在日常 `outputs/title-repair-acceptance/real-run/`。没有追加重跑、切换模型或购买额度。

用户另要求最多 2 条带型号补测。原两图项目未命中知识库，当前活动目录未检索到摩卡条目。另一个 ps691-b 项目的当前卖点库实际是“猫窝电动沙发床”，不能用它证明黑森林沙发匹配。已准备该项目文件名标注 PS691-B 的商品照片作为候选：项目型号与活动目录精确匹配，展示名称清理为“林氏黑森林沙发”；但照片自身无可读 SKU 或卖点文字，不能独立证明型号，也不能替代提取契约所需的逐字证据。历史“怀抱 实木沙发”详情图已做首尾本机视觉核对，尚未确认与 PS691-B 的真实对应。2 条 × 15 秒请求与本机来源元数据已准备，未提交外部模型任务；待确认同款详情资料及新增素材外发范围。原项目型号、目录和旧脚本未修改，记录在日常 `outputs/title-repair-acceptance/model-supplement/`。

## 读图负载排查（只读）

用户反馈中转后台有余额、服务显示正常。已核对当前监听进程继承环境及日常/standalone 环境文件中的提取参数，没有覆盖，读图配置为最多 4 并发、每次 6 切片、120 秒超时、最多 2 次尝试；两页顺序处理。离线重算为 38 + 27 个切片，完整无失败流程会有 12 个逻辑批次，六图请求约 1.08–1.93 MB（含 Base64 JSON）。这只是本机负载重建，不能当作本轮真实调用次数。

实际失败记录只保留一个“第 1–6 切片”批次的两次耗时与最终 503，没有页码；成功批级指标未在失败时落库，Gemini 不在公司模型 usage 账本覆盖范围，无法恢复全部真实请求轨迹。因此并发压力是待验证假设，不能认定为欠费、中转整体故障或限流。首稿 6 条尚未开始，不由脚本生成并发直接触发。

已准备不执行的最小串行对照：固定原两图中的第 1 页前 6 切片，先只改为 1 个六图请求，无自动重试；若仍失败，再把相同 6 切片拆为 3 个两图请求并保持串行。最多新增 4 次读图调用、0 条脚本，不改全局设置。请求审阅文件与安全参数证据在 `real-run/serial-probe-plan.json`、`concurrency-analysis.md`、`concurrency-config-proof.json`。当前用户是在问原因，本轮未据此新增调用。
