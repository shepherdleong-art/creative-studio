# Seedance 2.0 标准版与 Fast 分辨率实测

用户授权各生成一条真实任务，验证公司通道输出档位。两条均通过当前源码的 `openaiVideoAdapter.submit/poll`，经本机 LiteLLM（127.0.0.1:4000）、公司网关和 COS 素材中转完成；使用同一张本地合成的 768×1024 木柜示意图，不使用现有业务素材。

| 模型 | 请求秒数 | 实际请求 size | 原片宽×高 | 视频流时长 | 帧率 | 编码 / 像素格式 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `doubao-seedance-2-0-260128` | 5 | `1248x1664` | 1248×1664 | 5.041992s | 24fps | H.264 / yuv420p | 3:4 的 1080p，通过 |
| `doubao-seedance-2-0-fast-260128` | 5 | `834x1112` | 834×1112 | 5.041992s | 24fps | H.264 / yuv420p | 3:4 的 720p，通过 |

宽高来自下载原片的 ffprobe 视频流元数据，未经过放大或转码。实际 POST 使用 `size`，由公司通道映射到上游分辨率；没有额外发送方舟原生 `resolution` 字段。

本次每个模型仅提交一条单图任务。首尾帧请求与其余比例有本地请求回归覆盖，但不属于本次真实任务验证范围。2.5 未重新提交任务。

本机证据（gitignored）：`outputs/seedance-resolution-2026-09-21/results.json` 保存请求尺寸、任务编号、原片路径与 ffprobe 结果；同目录保存两条模型名命名的 MP4 与 `synthetic-3x4.png`。探测脚本为 `outputs/seedance-resolution-live.ts`，检查点用于避免重复提交。
