# Creative Studio 素材生产领域

Creative Studio 把产品素材、脚本文案和视频生产组织在本地项目中。本词汇表统一单条精准混剪与批量生产使用的业务语言。

## 项目与素材

**项目（Project）**：
围绕一个产品持续保存素材、脚本、生产记录和正式产物的工作空间。
_避免：批次、文件夹_

**分镜组（Shot Set）**：
属于同一套分镜语境的一组分镜、脚本和模块 4 视频；单条精准混剪以它作为素材隔离边界。
_避免：项目素材库、批次素材池_

**项目素材库（Project Media Library）**：
当前项目可以复用的素材目录；批次只能使用用户从中明确选择的素材。
_避免：公共素材池、分镜组素材_

**托管素材（Managed Media）**：
由 Creative Studio 保存和管理其文件副本的项目素材。
_避免：上传素材、内部素材_

**链接素材（Linked Media）**：
原文件留在用户文件夹或外置磁盘中，项目保存可核验引用的素材。
_避免：外部素材、临时素材_

**素材身份（Media Identity）**：
用于确认项目中一份素材内容连续性的稳定身份；同一项目中的同一内容只拥有一个素材身份，不随文件改名、移动或增加来源而改变。
_避免：文件名、文件路径_

**素材来源（Media Source）**：
项目找到同一素材内容的一个入口；一份素材可以同时拥有模块 4、托管副本和链接原文件等多个来源。
_避免：重复素材、文件身份_

**内容指纹（Content Fingerprint）**：
用于核验两个文件是否属于同一素材内容的特征；文件名、路径和快速筛选结果都不能单独代替最终核验。
_避免：文件名、路径 ID_

**离线素材（Offline Media）**：
项目仍保留记录、但当前没有任何可核验且可读取来源的素材；恢复已有来源或重新定位到同一内容后可以继续使用。
_避免：归档素材、已删除素材_

**内容已变化（Changed Media Source）**：
素材来源的路径仍然存在，但当前位置的文件已经无法核验为原素材内容。
_避免：离线素材、自动替换_

**重新定位（Relink）**：
在文件移动、改名或磁盘变化后，为既有素材来源重新选择并核验同一内容的位置。
_避免：重新导入、替换素材_

**模块 4 素材（Module 4 Media）**：
模块 4 成功生成并关联到项目与分镜组的视频素材。
_避免：AI 素材、生成结果_

**批次素材池（Batch Media Pool）**：
用户为某个批次明确选择的项目素材快照，范围只属于该批次。
_避免：项目素材库、自动素材池_

**归档素材（Archived Media）**：
已从正常素材列表移除、不再进入新批次，但为已有批次和历史记录保留身份的项目素材。
_避免：已删除素材、离线素材_

## 脚本

**项目脚本（Project Script）**：
由第 3 步「脚本生成」保存、拥有稳定身份并可在第 4 步视频生产和智能混剪中复用的脚本文案及标题；同一项目脚本可以持续产生新版本。
_避免：批次脚本、临时文案_

**项目脚本版本（Project Script Revision）**：
一份项目脚本在某次明确保存后形成的正文与标题版本；新批次可以采用最新版，已经开始的批次仍保留自己采用的旧版本。
_避免：重复脚本、脚本快照_

**批次脚本（Batch Script）**：
被用户选入某个批次的项目脚本或外部文案。
_避免：项目脚本、脚本原稿_

**脚本快照（Script Snapshot）**：
批次开始时固定的正文、标题和来源版本；上游后续修改不会静默改变它。
_避免：脚本副本、最新脚本_

**生成份数（Copy Count）**：
一份批次脚本要求产生的不同成片数量。
_避免：重试次数、并发数_

## 混剪生产

**单条精准混剪（Single Precise Mixcut）**：
在一个分镜组边界内，对单条成片进行细致生成和调整的模式。
_避免：单条模式、旧混剪_

**批量生产（Batch Production）**：
使用多份脚本和一个明确选择的批次素材池，统一规划并生成多条成片的模式。
_避免：多生成几条、批量导出_

**批次（Batch）**：
一次批量生产的完整工作单，包含输入快照、默认设置、成片计划、任务状态和正式产物。
_避免：项目、会话、渲染任务_

**批次版本（Batch Revision）**：
同一批次在一次生产启动时固定的素材、脚本和默认设置集合；整体输入发生变化时形成新版本，旧版本仍可追溯。
_避免：重跑批次、覆盖批次_

**成片计划（Output Plan）**：
一条目标成片在正式渲染前确定的脚本、素材区间、顺序、封面和音乐安排。
_避免：时间线副本、渲染任务_

**成片版本（Output Revision）**：
同一成片计划经过局部调整后形成的可追溯版本；它不改变同批次中的其他成片。
_避免：新任务、覆盖成片_

**生产任务（Production Task）**：
批次中一次可追踪的处理工作，服务于素材准备或某条成片，不代表用户要求增加一条成片。
_避免：成片计划、成片卡片_

**任务尝试（Task Attempt）**：
生产任务的一次实际执行记录；失败后的重试形成新的尝试，但不增加成片计划数量。
_避免：生成份数、新成片_

**任务进度（Task Progress）**：
生产任务实际达到的阶段、已完成数量或可测百分比；无法测量的远端阶段只说明正在做什么，不伪造百分比。
_避免：动画进度、预计完成度_

**生产暂停（Production Pause）**：
一批生产暂时不再开始新工作，并请求正在执行的工作进入可恢复状态；已完成结果和历史尝试继续保留。
_避免：停止生产、关闭窗口_

**生产停止（Production Stop）**：
用户结束一批生产中所有未完成工作的最终选择；已经完成并发布的成片继续保留。
_避免：暂停生产、删除批次_

**任务恢复（Task Recovery）**：
应用异常退出或主动暂停后，根据持久记录确认旧尝试结果并继续未完成生产的过程。
_避免：从头生成、重复提交_

**性能模式（Performance Mode）**：
用户决定本批任务按全速或降低速度运行的选择；默认全速，普通性能压力只触发提醒，不由系统静默切换。
_避免：电脑等级、自动档位_

**资源保护（Resource Protection）**：
项目盘确定不足或系统内存进入危险状态时，系统停止领取新的重任务并保存可恢复状态；普通高负载只提醒用户选择，不属于资源保护。
_避免：生产暂停、任务失败_

**成片卡片（Output Card）**：
批量生产中用于检查一条目标成片的主要用户界面单位。
_避免：视频任务、缩略图_

**批量生产工作区（Batch Production Workspace）**：
一批生产从准备、运行、暂停到检查始终使用的持久界面；它汇总批次、成片计划、任务和正式产物，但不是任务状态表或临时浏览器会话。
_避免：批量页面、任务列表_

**成片卡片状态（Output Card State）**：
根据成片计划的当前版本、可用正式产物、必要任务、阻塞和提醒汇总出的用户状态；它不直接等于某一条生产任务的状态。
_避免：任务状态、最后一次尝试_

**可导出成片（Exportable Output）**：
已经通过渲染、校验和发布登记，可以安全播放或交付的当前正式产物；临时文件、失败输出和未校验文件不属于可导出成片。
_避免：已渲染文件、预览文件_

**正式产物（Published Artifact）**：
通过校验并登记到项目中、可供用户交付使用的视频或封面文件。
_避免：预览文件、中间文件_

**当前成片（Current Artifact）**：
一张成片卡片当前默认代表的正式产物；历史正式产物仍然保留并可重新设为当前成片。
_避免：唯一成片、最后一个文件_

**部分完成（Partial Completion）**：
批次中的一部分成片已经成功、一部分仍失败或未完成的稳定结果状态；成功成片可以继续使用，失败项仍可单独重试。
_避免：批次失败、全部完成_

**分配运行（Allocation Run）**：
联合分配器针对一个冻结批次版本和规则版本形成的一次可复现计算记录；它产生候选成片版本和差异报告，但不是渲染任务或正式产物。
_避免：生成任务、成片版本_

**片段锁定（Segment Lock）**：
用户要求自动分配继续保留的一项成片镜头决定，绑定到明确的脚本句段、素材身份和来源区间，直到用户明确解除。
_避免：收藏素材、永久占用_

**差异报告（Variation Report）**：
说明同批成片在时间线、开头、素材、区间、顺序、封面和音乐上的重合事实及降级原因；它用于提示风险，不承诺素材不足时仍然低相似。
_避免：质量评分、唯一相似度_

## 媒体处理

**素材分析版本（Media Analysis Revision）**：
一份素材在某次分析中形成的可复用理解结果；批次引用实际采用的版本，后续重分析不改写旧批次依据。
_避免：批次分析、实时分析结果_

**代理文件（Proxy Media）**：
只用于降低预览负担、可以集中清理和重新生成的素材副本；它可以包含用户选定的 LUT 预览效果，但不参与最终导出。
_避免：压缩原片、低清成片_

**LUT 资产（LUT Asset）**：
用户导入并通过本地验证、由 Creative Studio 受管保存的色彩转换文件；同名但内容不同的文件拥有不同身份。
_避免：滤镜文件、调色预设_

**LUT 应用（LUT Application）**：
用户为批次中的指定素材明确选择的非破坏性色彩转换；预览使用匹配代理，最终输出重新作用于原片。
_避免：自动调色、修改原片_

## 桌面与升级

**桌面运行壳（Desktop Runtime Host）**：
负责窗口、单实例、本机文件选择以及看管私有 Node 服务的薄 Electron 层；它不承载混剪业务、SQLite 或 FFmpeg 实现。
_避免：Electron 后端、第二套应用_

**公司供应商运行环境（Company Provider Runtime）**：
只在公司内网启用、由统一启动入口看管的可选供应商环境；本地 LiteLLM 是连接公司模型中转站的必要桥接，但其故障只应禁用公司供应商，不能阻塞工作台或外部供应商。
_避免：应用必需后端、所有供应商代理_

**媒体传输 Adapter（Media Transport Adapter）**：
把当前任务明确选择的图片、视频或抽帧结果以受控方式提供给公司模型链路的可替换边界；当前 Cloudflare 仅为开发期实现，未来腾讯云方案需限制范围、授权过期并支持清理。
_避免：公开整个工作台、永久素材公网地址_

**迁移备份（Migration Backup）**：
数据库执行一个新 schema 版本之前，通过 SQLite 一致性备份能力生成并完成完整性核验的快照；它不等于在运行时普通复制一个数据库主文件。
_避免：数据库副本、媒体备份_

**兼容模式（Compatibility-only Mode）**：
新功能 schema 未能安全启用时，只开放已经确认不依赖新结构的旧项目与单条能力，批量功能保持关闭的恢复状态。
_避免：降级成功、忽略迁移错误_

**内部测试包（Internal Test Build）**：
只用于指定电脑上的安装与功能验收、可能尚未完成正式签名或公证的桌面产物；不能描述为可公开分发版本。
_避免：正式安装包、发布版_
